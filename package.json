{
  "name": "vibe4fpga",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/vscode-extension"
  ],
  "scripts": {
    "build": "npm run compile --workspace=packages/vscode-extension",
    "watch": "npm run watch --workspace=packages/vscode-extension",
    "lint": "npm run lint --workspace=packages/vscode-extension"
  }
}
