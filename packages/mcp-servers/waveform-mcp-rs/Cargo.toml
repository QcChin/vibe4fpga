[package]
name = "waveform-mcp-rs"
version = "0.2.0"
edition = "2021"
description = "MCP server for VCD/FST waveform file parsing, written in Rust. Exposes 4 pure-parsing tools (parse_waveform, extract_signal_events, get_signal_stats, summarize_waveform)."
license = "MIT"

[[bin]]
name = "waveform-mcp-rs"
path = "src/main.rs"

[dependencies]
rmcp = { version = "0.1", features = ["server", "transport-io"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
anyhow = "1"
vcd = "0.7"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
