"""Skills absorbed from the retired ``packages/skills/`` package.

Current skills:
    * :mod:`.testbench_gen` — natural-language spec → SystemVerilog testbench
    * :mod:`.verification`  — multi-stage scoring (lint + sim + formal + synth + spec)
"""
