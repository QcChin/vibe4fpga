"""Skill implementations absorbed from the retired ``packages/skills/`` package.

Current skills:
    * :mod:`.waveform_debug` — 5 pure-Python detectors (glitches, X/Z, CDC,
      AXI handshake, stall timeout) + LLM reasoning layer
"""
