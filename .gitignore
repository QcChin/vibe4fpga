# Node.js
node_modules/
out/
dist/
*.vsix
.vscode-test/
.npm/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv
env/
venv/
ENV/
.uv/

# uv lockfiles — not committed; each contributor's install resolves its own
uv.lock

# IDE
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build outputs
*.js.map
*.d.ts.map

# Test / coverage
.pytest_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml

# Vector DB storage
qdrant_storage/

# Secrets
*.env.local
.env.*.local

# Claude — user-local overrides (the committed CLAUDE.md is force-added)
.claude/settings.local.json
# Historical note: CLAUDE.md used to be ignored so every contributor could
# keep a personal one. A committed baseline now lives at the repo root
# (force-added); uncomment the line below if you want to hide local edits.
# CLAUDE.md
