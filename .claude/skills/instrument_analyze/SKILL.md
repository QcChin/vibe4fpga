---
name: instrument_analyze
description: Compare oscilloscope capture against simulation, classify waveform diffs into 7 categories (overshoot / offset / noise / drift / phase / amplitude / unknown), and recommend root-cause investigations.
trigger: oscilloscope|scope|instrument|diff|classify waveform
---

# instrument_analyze

Compare oscilloscope capture against simulation, classify waveform diffs into 7 categories (overshoot / offset / noise / drift / phase / amplitude / unknown), and recommend root-cause investigations.

## MCP backing

| | |
| --- | --- |
| Server | `instrument-mcp` |
| Tool | `analyze_instrument_diff` |
| Transport | stdio |
| Binary | `instrument-mcp` |

## Environment


**Optional:**
- `VIBE4FPGA_LLM`
- `ANTHROPIC_API_KEY`
- `ANTHROPIC_AUTH_TOKEN`
- `ANTHROPIC_BASE_URL`
- `OPENAI_API_KEY`
- `OPENAI_BASE_URL`


## How Claude Code should use this skill

Invoke the MCP tool `instrument-mcp.analyze_instrument_diff` when the user's
intent matches the trigger above.

Accepts raw ``list[float]`` arrays OR file paths (CSV + VCD) OR a VISA
resource string for live SCPI capture. When VISA drivers are absent the
SCPI path surfaces a structured ``RuntimeError`` from ``_require_pyvisa``
— the CSV/array paths still work for Mac developers without NI-VISA.


<!-- Generated by tools/gen-skills/generate.py — do not hand-edit. -->
