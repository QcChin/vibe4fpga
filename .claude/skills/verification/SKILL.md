---
name: verification
description: Score a verification run across 5 stages (lint / sim / formal / synth / spec) and emit a Markdown narrative report.
trigger: verify|score|verification report
---

# verification

Score a verification run across 5 stages (lint / sim / formal / synth / spec) and emit a Markdown narrative report.

## MCP backing

| | |
| --- | --- |
| Server | `verify-mcp` |
| Tool | `score_verification` |
| Transport | stdio |
| Binary | `verify-mcp` |

## Environment


**Optional:**
- `VIBE4FPGA_LLM`
- `ANTHROPIC_API_KEY`
- `ANTHROPIC_AUTH_TOKEN`
- `ANTHROPIC_BASE_URL`
- `OPENAI_API_KEY`
- `OPENAI_BASE_URL`


## How Claude Code should use this skill

Invoke the MCP tool `verify-mcp.score_verification` when the user's
intent matches the trigger above.

<!-- Generated by tools/gen-skills/generate.py — do not hand-edit. -->
