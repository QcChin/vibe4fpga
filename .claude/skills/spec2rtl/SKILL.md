---
name: spec2rtl
description: Convert a natural-language FPGA specification into synthesizable Verilog via a 5-stage pipeline (parse → ambiguity → context → generate → self-check).
trigger: spec|rtl|verilog|synthesize from english|natural language
---

# spec2rtl

Convert a natural-language FPGA specification into synthesizable Verilog via a 5-stage pipeline (parse → ambiguity → context → generate → self-check).

## MCP backing

| | |
| --- | --- |
| Server | `fpga-project-mcp` |
| Tool | `spec_to_rtl` |
| Transport | stdio |
| Binary | `fpga-project-mcp` |

## Environment


**Optional:**
- `VIBE4FPGA_LLM`
- `ANTHROPIC_API_KEY`
- `OPENAI_API_KEY`


## How Claude Code should use this skill

Invoke the MCP tool `fpga-project-mcp.spec_to_rtl` when the user's
intent matches the trigger above.

Stage 3 (context injection) pulls naming conventions from the host's own
scanner — pass ``project_path`` in the tool call to activate. BLOCKING
ambiguities pause the pipeline and surface the open questions; ADVISORY
ambiguities are resolved autonomously and recorded in the audit trail.


<!-- Generated by tools/gen-skills/generate.py — do not hand-edit. -->
