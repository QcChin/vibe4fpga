---
name: {{ skill.opencode_command or skill.id }}
description: {{ skill.summary }}
---

# /{{ skill.opencode_command or skill.id }}

{{ skill.summary }}

Routed to MCP server **`{{ mcp.mcp_name }}`** (binary `{{ mcp.binary }}`, {{ mcp.transport }} transport), tool `{{ skill.tool }}`.

{% if mcp.env and (mcp.env.required or mcp.env.optional) -%}
## Required environment

{% if mcp.env.required -%}
{% for v in mcp.env.required -%}
- `{{ v }}` (required)
{% endfor %}
{%- endif %}
{% if mcp.env.optional -%}
{% for v in mcp.env.optional -%}
- `{{ v }}` (optional)
{% endfor %}
{%- endif %}
{%- endif %}

<!-- Generated by tools/gen-skills/generate.py — do not hand-edit. -->
